📘 store folder
----------------
Holds Zustand (or Redux) stores for state management.

Purpose:
- Manage app-level data that must persist between pages or reloads.

Main store for MVP:
- useSetupStore.js → tracks setup flow data (profile, accounts, budget, goals, buckets)

Future stores:
- useUserStore.js → authentication / profile data
- useDashboardStore.js → dashboard analytics state
